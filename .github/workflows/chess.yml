name: Chess Workflow on Issue

on:
  issues:
    types: [opened]

jobs:
  Make-a-move:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Get chess move notation from issue
        run: |
          echo "ISSUE_TITLE=${{ github.event.issue.title }}" >> $GITHUB_ENV

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: "3.x"
          cache: "pip"

      - name: Generate Chessboard Image and update readme
        run: |
          # Install dependencies
          pip install -r scripts/requirements.txt

          # generate new chessboard image and export .env variables
          # parameters: issue_title, author
          python scripts/export_chessboard.py "$ISSUE_TITLE" "${{ github.event.issue.user.login }}"

          # export environment variables from .env file
          while IFS= read -r line; do
            echo "$line" >> $GITHUB_ENV
          done < <(grep -v '^#' .env)

          if [ ! -f "chessboard.png" ]; then
            echo "Move Invalid, no chessboard.png file generated"
            exit 1
          fi

          # Get game number from .env
          GAME_NUMBER="${{ env.GAME_NUMBER }}"

          # if gamenumber is not set, exit
          if [ -z "$GAME_NUMBER" ]; then
            echo "Game number not set, exiting"
            exit 1
          fi

          # rename file to chessboard.png + timestamp
          TIMESTAMP=-$(date +%s)
          mv -f chessboard.png games/game${GAME_NUMBER}/chessboard$TIMESTAMP.png

          # add Filename to github_env
          echo "CHESSBOARD_FILENAME=games/game${GAME_NUMBER}/chessboard$TIMESTAMP.png" >> $GITHUB_ENV

          # Check if game has ended and initialize next game
          if [ "${{ env.GAME_STATUS }}" != "in_progress" ]; then
            GAME_NUMBER=$((GAME_NUMBER + 1))
            python scripts/export_chessboard.py $GAME_NUMBER
          fi

      - name: Commit changes
        if: ${{ env.MOVE_STATUS == 'valid' }}
        run: |
          # parameters: moves, image_url, link_url
          python scripts/update_readme.py "${{ env.GAME_HISTORY }}" "https://raw.githubusercontent.com/thieleju/thieleju/main/${{ env.CHESSBOARD_FILENAME }}" "${{ env.VALID_MOVES }}" 

          # commit changes
          git config --global user.email "actions@github.com"
          git config --global user.name "GitHub Actions"

          git add .
          git commit -m "🚀 Update chessboard image, readme and pgn"
          git push origin HEAD

      # - name: Update issue
      #   if: ${{ env.MOVE_STATUS == 'valid' && env.GAME_STATUS == 'in_progress' }}
      #   uses: actions/github-script@v7
      #   with:
      #     script: |
      #       const moveHistory = "${{ env.GAME_HISTORY }}".replace(/\.\s/g, ".&nbsp&nbsp").replace(/\|/g, "<br>")
      #       const gifUrl = "https://raw.githubusercontent.com/thieleju/thieleju/main/games/game${{ env.GAME_NUMBER }}/game.gif"
      #       const issueBody = `
      #       ### Comment on this issue to make a move! It's ${{ env.WHICH_TURN }}'s turn to move.
      #       > Move has to be in Standard Algebraic Notation ([SAN](https://www.chess.com/terms/chess-notation)) (e4, Bf4, exd5, Qxf7+, ...) <br>
      #
      #       <div>
      #         <img src="https://raw.githubusercontent.com/thieleju/thieleju/main/${{ env.CHESSBOARD_FILENAME }}" \>
      #       </div>
      #
      #       ### Legal moves for ${{ env.WHICH_TURN }}:
      #       > ${{ env.VALID_MOVES }}
      #
      #       <details>
      #         <summary>Show Move History</summary><br>
      #         <div>
      #           <img width="350" src="${gifUrl}" \>
      #         </div>
      #         ${moveHistory}
      #       </details>
      #       `
      #       await github.rest.issues.update({
      #         issue_number: context.issue.number,
      #         owner: context.repo.owner,
      #         repo: context.repo.repo,
      #         body: issueBody
      #       })

      - name: Reply to issue (Game in Progress)
        if: ${{ env.GAME_STATUS == 'in_progress' }}
        uses: actions/github-script@v7
        with:
          script: |
            const commenter = context.payload.comment.user.login;

            if ("${{ env.MOVE_STATUS }}" == 'valid') {
              const replyBody = "Your move has been processed and the chessboard has been updated. Thank you for playing, @${{commenter}}! 🚀";

            } else if ("${{ env.MOVE_STATUS }}" == 'invalid') {
              const replyBody = "Your move was invalid. Please make a valid move in Standard Algebraic Notation ([SAN](https://www.chess.com/terms/chess-notation)) (e4, Bf4, exd5, Qxf7+, ...). Thank you for playing, @${{commenter}}! 🚀";

            } else {
              const replyBody = "Something went wrong. :confused: Please try again.";
            }

            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: replyBody
            });

      - name: Reply to issue (Game Ended)
        if: ${{ env.GAME_STATUS != 'in_progress' }}
        uses: actions/github-script@v7
        with:
          script: |
            const moveHistory = "${{ env.GAME_HISTORY }}".replace(/\.\s/g, ".&nbsp&nbsp").replace(/\|/g, "<br>")
            const gifUrl = "https://raw.githubusercontent.com/thieleju/thieleju/main/games/game${{ env.GAME_NUMBER }}/game.gif"
            const gameResult = ""
            const gameResultLabel = ""

            switch (${{ env.GAME_STATUS }}) {
              case "white_wins":
                gameResult = "White Won"
                gameResultLabel = "white"
                break;
              case "black_wins":
                gameResult = "Black Won"
                gameResultLabel = "black"
                break;
              case "stalemate":
              case "insufficient_material":
              case "threefold_repetition":
              case "draw":
                gameResult = "Game ended in a draw"
                gameResultLabel = "draw"
                break;
              default:
                gameResult = "Invalid result :("
            }
            const replyBody = `
            ### ${gameResult} 

            ### Move History
            <div>
              <img width="350" src="${gifUrl}" \>
            </div>
            ${moveHistory}

            Thank you all for playing!
            `

            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: replyBody
            });

            await github.rest.issues.addLabels({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              labels: ['chess', gameResultLabel]
            })
